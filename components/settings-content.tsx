'use client';

import { Typography } from 'antd';

const { Title } = Typography;

export default function SettingsContent() {
  return (
    <>
      <Title level={4}>系统设置</Title>
      <div>这里后面再补系统设置内容。</div>
    </>
  );
}
